基于修改后的 get_requirement_items 函数，完整的匹配规则总结如下（共五个核心阶段）：

1. 数据分组规则（按顺序 + 相同 Comments）
分组依据：按照数据库读取顺序（id 升序），将 Comments 字段连续相同的记录归为同一个需求组。

组内标识：组内所有记录共享同一个需求量，且只要组内 任意一条 记录的 Require_State 为 'Must'，则整组标记为“必须”（否则为“可选”）。

💡 替代逻辑说明：若您的“合并标识”是指特定的标记文本（例如含有 "合并" 字样的记录作为结束点），可将分组条件从 group['comments'] != comments 修改为对应的标记判断，当前逻辑按“连续相同值”执行。

2. 需求数量解析规则
优先取数：提取该组 第一条 记录的 Comments 字段中的第一个数字（正则 \d+），作为该组的总需求量。

默认兜底：若 Comments 中未提取到任何数字，则默认需求量为 该组包含的记录条数（len(records)）。

3. Must / Optional 判定规则
Must（强制）：组内至少有一条记录的 Require_State 不区分大小写等于 'must'。

若匹配到的设备数量 少于 需求量，系统会记录警告日志，但仍会返回已匹配到的设备。

Optional（可选）：组内所有记录均非 'must'。

尽可能选取设备，若匹配到的设备数量不足，不会报错，只返回现有匹配结果。

4. 候选设备匹配规则（三层降级匹配）
针对组内 每一条记录 的 Category + Class + Type（拼接为字符串 match_str），按以下优先级匹配库存设备（DeviceLNV 表，已过滤掉 DevStatus 为 Damaged/Lost 的设备）：

优先级	匹配方式	具体逻辑
① 包含匹配	检查 match_str 是否包含在设备的组合字段中（IntfCtgry、DevCtgry、Devproperties、DevName、DevModel、DevVendor 全部转为小写拼接）。	只要存在包含关系即匹配成功。
② 单词匹配	当①无结果时，将 match_str 按空格和 | 拆分为单词（长度>2）。
- 全词匹配：所有单词均出现在设备的组合字段中。
- 任意词匹配（全词失败时）：只要任意一个单词出现即匹配。	降级处理，提高召回率。
③ 模糊匹配	当①②均无结果时，使用 difflib.get_close_matches，截断值 cutoff=0.8，从设备的模糊匹配字符串（不含 Vendor）中查找相似度最高的前 10 个。	最后手段，处理名称差异较大的情况。
组内所有记录的匹配结果会 合并去重（按 NID），形成该组的最终候选设备池。

5. 设备精选与分配规则（择优 + 分散厂商）
从候选设备池中选取最终所需数量的设备，遵循以下策略：

状态优先级排序：按设备状态排序，优先级为 Good > Fixed > Long（其他状态排最后）。

厂商分散策略（避免选用同一厂商的设备）：

将所有候选设备按 DevVendor 分组。
采用 轮询 方式，依次从不同厂商中各取 1 台设备。
若轮询完所有厂商仍未满足需求量，则从剩余设备中 按顺序补充（不限制厂商）。
数量校验：

若为 Must 且最终选中数量 < 需求量，记录警告。

若为 Optional，实际选中数量可能小于需求量，直接返回现有结果。

最终输出
每组最终选中的设备，都会生成一条需求项 (type_key, NID)，其中 type_key = (IntfCtgry, DevCtgry, Devproperties)，用于后续按周统计设备需求量。